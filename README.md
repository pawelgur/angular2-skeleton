# Angular 2 infrastructure playgroind

Trying out different angular v2 build and infrastructure stuff - building, bundling, aot, lazy loading, etc

## Installation
`npm i`

## Running
`npm run start`
